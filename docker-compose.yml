services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: angular_nginx_second:latest
    container_name: angular_nginx_second_app
    ports:
      - "9393:80"
    environment:
      - NODE_ENV=production
    networks:
      - internalnet

networks:
  internalnet:
    driver: bridge



